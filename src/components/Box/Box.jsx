import styled from 'styled-components';
import { layout, space, flexbox, color, borders } from 'styled-system';

export const Box = styled.div`
  ${layout}
  ${flexbox}
  ${space}
  ${color}
  ${borders}
`;
